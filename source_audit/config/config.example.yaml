# Copy to config.yaml and adjust. Do not commit config.yaml if it contains secrets.

source:
  base_url: "https://voir-anime.to/"

http:
  timeout_seconds: 15
  max_retries: 2
  retry_backoff_seconds: 1.5
  user_agent: "source_audit-research-bot/0.1 (+contact: lionelyvan24@gmail.com)"

evidence:
  output_dir: "output"
  save_raw_html: true
  save_screenshots: false

logging:
  level: "INFO"
